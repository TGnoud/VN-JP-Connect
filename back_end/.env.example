# Copy this file to .env for local development.
# MONGO_URI can point to the shared MongoDB Atlas database if the team needs it.
MONGO_URI=mongodb+srv://<db_username>:<db_password>@vn-jp-connect.eraedra.mongodb.net/?appName=VN-JP-CONNECT

# Use this variable when running npm run seed.
# Keep it local, or use a database name ending with _local.
MONGO_LOCAL_URI=mongodb://localhost:27017/vn_jp_connect_local
